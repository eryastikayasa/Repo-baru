name: Build ESP32-S3 Firmware

on:
  push:
    branches:
      - main
      - fix/geminiproject
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # ============================================================
      # CHECKOUT
      # ============================================================
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      # ============================================================
      # PYTHON
      # ============================================================
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # ============================================================
      # PIP
      # ============================================================
      - name: Upgrade pip
        run: python -m pip install --upgrade pip

      # ============================================================
      # PLATFORMIO
      # ============================================================
      - name: Install PlatformIO
        run: pip install platformio

      - name: Check PlatformIO
        run: pio --version

      - name: List available ESP32-S3 boards
        run: pio boards | grep esp32-s3-devkitc

      # ============================================================
      # CHECK ESPRESSIF32 / ESP-IDF
      # ============================================================
      - name: Check Build Environment
        run: |
          pio platform show espressif32
          pio pkg list
          
          pio run -t clean
          pio run

      # ============================================================
      # BUILD
      # ============================================================
         
      
      - name: Build ESP32-S3
        run: pio run

      # ============================================================
      # VERIFY BUILD OUTPUT
      # ============================================================
      - name: Verify Firmware
        run: |
          test -f .pio/build/esp32-s3-devkitc-1/firmware.bin
          test -f .pio/build/esp32-s3-devkitc-1/bootloader.bin
          test -f .pio/build/esp32-s3-devkitc-1/partitions.bin

      # ============================================================
      # ARTIFACTS
      # ============================================================
      - name: Upload firmware artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ESP32S3-Firmware-Full
          if-no-files-found: error
          path: |
            .pio/build/esp32-s3-devkitc-1/firmware.bin
            .pio/build/esp32-s3-devkitc-1/bootloader.bin
            .pio/build/esp32-s3-devkitc-1/partitions.bin
